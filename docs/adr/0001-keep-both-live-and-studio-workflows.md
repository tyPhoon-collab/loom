# Keep both live and Studio workflows

Loom keeps `loom live` as the external-editor live coding workflow and `loom studio` as the integrated in-app editing workflow. Studio is intended to be sufficient for most composition work by combining playback, source editing, and structure-aware operations, but it does not replace users' preferred external editors.
