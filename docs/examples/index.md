# Examples

Examples live in the repository `examples/` directory.

## Categories

- feature checks (`feature-*.loom`)
- musical demos (`drums-*.loom`, `melody-*.loom`, etc.)
- internal reproduction cases (`examples/internals/*`)

This section will gain narrative walkthrough pages.
