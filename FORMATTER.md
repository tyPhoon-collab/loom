# Loom Formatter Specification

## 鉄の掟

1. **Bar は縦にそろえる** — フォーマッタの存在意義
2. **左寄せ** — 中央揃えは採用しない
3. **フォーマットはブロック内で完結** — 別トラック・別ブロックに影響しない
4. **セマンティクスを変えない** — ブロック数・バー数・トークン数は入力のまま保持

## 垂直方向の整形（Islands of Meta）

Loom は、楽器やコメントといった「メタ情報」を独立した島（Island）として扱うことで、スコアの視認性を高めます。

- **メタ要素の独立**: `TrackHeader`, `Comment`, `TrackWrap`, `Frontmatter` は、その上下に必ず1行の空行を伴って配置されます。
- **演奏データの密着**: `Pattern` と `Modifier` は、一つの演奏ブロックとして空行を挟まずに密着します。
- **空行の集約**: ユーザが入力した連続する空行は、ルールに基づき常に「1行のみ」に集約（Squeeze）されます。
- **クリーンアップ**: すべての行から行末スペースが削除され、ファイル末尾は必ず改行で終わります。

## テンプレートの整形 (Template Expansions)

1行に記述した複数のテンプレート展開（例: `[@a][@b]`）は、それぞれの展開が独立しつつ、横並びのスペース区切り（例: `[@a] [@b]`）で出力されます。
また、パイプ (`|`) や大括弧 (`[]`) 周りの余計な空白は除去され、可能な限りコンパクトでクリーン（例: `[@note|x9|+4]`）に保たれます。

## Loom Grid アルゴリズム

Loom Formatter は、音楽的な「拍」のアライメントと、横幅の爆発防止を両立する単一のアルゴリズムを採用しています。

### 1. グリッドサイズ ($G$) の決定

ブロック列内の各トラックのトークン数 $K_i$ の最小公倍数 ($LCM$) をベースに算出します。

- 密度が高いトラックでも視認性を確保するため、$LCM$ の整数倍のうち、$K_{max} \times 2$ 以上となる最小の値を $G$ とします。
- ただし、$G$ が 24 を超える場合は 24 でクランプ（量子化）します。
- $G$ は常に $K_{max}$ 以上であることが保証されます。

### 2. パディングと配置

各ブロックは合計 $G + 2$ 個のカラムを持ちます。

- **先頭パディング**: 常にインデックス 0 は空白。
- **グリッド配置**: $K$ 個のトークンを持つトラックの $j$ 番目（0開始）のトークンは、インデックス $1 + \lfloor j \times G / K \rfloor$ に配置。
- **末尾パディング**: グリッドの後に Bar までの余白を維持。

### 3. スロット幅の同期（Elastic Width）

Loom は固定幅のスプレッドシートではなく、**楽譜（Score）のような伸縮性**のあるレイアウトを採用しています。

- **カラム幅の動的決定**: 各カラム（グリッド位置）の幅は、その列に存在する「最も長いトークンまたは数値」に合わせて決定されます。
- **情報の高密度化**: 和音（Multi-note）や長いモディファイア（`v|!100`など）がある列だけが自然に広がり、単純な単音の列は詰まります。
- **垂直アライメントの厳守**: カラムごとの幅が異なっても、同じインデックスに属する全トラックの情報は垂直に完璧に揃います。

### 4. Group の扱い

`[...]`（Group）はグリッド計算上「1つのトークン」として扱われます。
Group 内部でさらにグリッドを細分化してアライメントすることはないため、複雑な連符や装飾音が含まれても、ブロック全体のグリッドが不必要に爆発することはありません。Group は、そのスロットが必要な幅をすべて占有することで、内部の整合性を保ちます。
