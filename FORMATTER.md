# Loom Formatter Specification

## 鉄の掟

1. **Bar は縦にそろえる** — フォーマッタの存在意義
2. **左寄せ** — 中央揃えは採用しない
3. **フォーマットはブロック内で完結** — 別トラック・別ブロックに影響しない
4. **セマンティクスを変えない** — ブロック数・バー数・トークン数は入力のまま保持

## 概念

### ブロック

Bar で囲まれた部分。同じブロック列（同一トラック内の同位置）は縦にそろえる。

### スロット

1つのトークンを配置するスペース。

#### スロット幅の決定

スロット幅は以下のルールで決まる:

1. **Pattern行**: Group 以外のトークンの表示幅は常に **1**（`^`, `.`, `-`）
2. **Modifier行**: 値の表示幅（最大5文字, 例: `!-100`）
3. **統一スロット幅**: ブロック列内の**非Groupスロット**の modifier 値の最大幅で統一（Group 位置の modifier 値は含めない）

#### Group のスロット幅への影響

**フォーマッタごとに Group の扱いが異なる**:

| モード | Group → 他スロット波及 | 理由 |
|--------|----------------------|------|
| minimize | しない | 機械的配置。Group は独自幅 |
| justify | しない | 機械的配置。Group は独自幅 |
| equal | しない | 按分配置。Group は独自幅 |
| **time** | **する** | 1スロット = 1時間単位。等幅必須 |

```
C3 | ^  ^  ^  [^ ^ ^] |
v  | 40 60 80 !100    |
```

| Slot | Pattern | Modifier | スロット幅 | 備考 |
|------|---------|----------|-----------|------|
| 0    | `^` (1) | `40` (2) | 2         | 非Group: 統一幅 = max(2, 2, 2) |
| 1    | `^` (1) | `60` (2) | 2         | 非Group: 統一幅 |
| 2    | `^` (1) | `80` (2) | 2         | 非Group: 統一幅 |
| 3    | `[^ ^ ^]` (7) | `!100` (4) | 7 | Group: 独自幅 (modifier値4より大きい) |

> [!NOTE]
> Time モードでは上記のルールに加え、Group 内のトークン数がグリッド解像度に影響する。
> 詳細は [Time](#time) セクションを参照。

## フォーマットモード

デフォルトは `equal`。

### 2つのカテゴリ

#### 機械的にそろえる

スロット間のスペースを機械的に配置する。

- **minimize**: スロット幅 + 1スペース（最小区切り）
- **justify**: ブロック内でスロットを均等分散

#### 視覚的にそろえる

時間軸を意識した配置。

- **time**: LCM でグリッドを算出し、NoteOn タイミングを時系列で並べる（ピアノロール）
- **equal**: time の横幅爆発を抑えるモード。per-slot で按分

### Minimize

スロット間を最小のスペースで区切る。Group は独自幅。

```
C3    | ^ ^ ^ ^               |
F4,C5 | ^ ^ ^ ^ [^ ^ [^ ^ ^]] |
```

### Equal

最大トークン数のスロットグリッドに対して、各行のトークンをインデックス比で按分配置。Group は独自幅。

```
F4,C5 |           | ^ ^ ^ ^ [^ ^ [^ ^ ^]] |
G4,B4 | ^ ^ ^ ^ ^ |                       |
E3    | .   ^   . | .   ^   .             |
C3    | ^   -   - |                       |
```

### Justify

ブロック内でスロット間を均等分散する。Group は独自幅。

```
F4,C5 |           | ^ ^ ^ ^ [^ ^ [^ ^ ^]] |
G4,B4 | ^ ^ ^ ^ ^ |                       |
E3    | .   ^   . | .         ^         . |
C3    | ^   -   - |                       |
```

### Time

LCM グリッドで時系列配置。ポリリズムの正確な視覚化。

**Group がグリッド解像度に影響する**:

- 各行の「実効グリッド解像度」= トークン数 × 最大Group内トークン数
- ブロック列の LCM はこの実効解像度で算出
- Group は複数グリッドスロットにまたがって `[...]` としてレンダリング
- **全スロット等幅**（Group が幅を波及させる）

```
# Drums: 10
snare | .     ^     .     ^     |
kick  | ^     .     [^ ^] .     |
```

上記のブロック2:
- snare: 4 tokens, max_group_size=1 → 実効 4
- kick: 4 tokens, max_group_size=2 → 実効 8
- LCM(4, 8) = 8 grid slots

```
G4,B4 | ^           ^           ^           ^           ^           |
E3    | .                   ^                   .                   |
C3    | ^                   -                   -                   |
```
